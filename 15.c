#include <stdio.h>
 
int main(void)
{
    int a, i, flag = 0, count = 0;
    scanf("%d", &a);
   for (i > 2; i < a; i++)
{
    if (a % i == 0)
    {
    count++;

     } 
    
     printf ("%d %d\n", count);

    return 0; 
}
     
}

